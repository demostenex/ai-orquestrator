use anyhow::Result;

/// Ponto de entrada do modo TUI interativo.
/// Por enquanto é apenas um placeholder que será expandido na Fase 1.
pub async fn start() -> Result<()> {
    println!("🚀 Bem-vindo ao AI Orchestrator (Modo Interativo)");
    println!("Esta é a versão inicial do TUI. Mais funcionalidades virão em breve.");
    println!("(Implementação da Fase 1 do pivô para TUI em andamento)");
    Ok(())
}